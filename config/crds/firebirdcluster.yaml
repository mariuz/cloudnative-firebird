apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: firebirdclusters.firebird.cloudnative-firebird.io
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.0
spec:
  group: firebird.cloudnative-firebird.io
  names:
    kind: FirebirdCluster
    listKind: FirebirdClusterList
    plural: firebirdclusters
    singular: firebirdcluster
    shortNames:
      - fb
      - fbc
    categories:
      - all
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      subresources:
        status: {}
        scale:
          specReplicasPath: .spec.instances
          statusReplicasPath: .status.instances
      additionalPrinterColumns:
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
        - name: Instances
          type: integer
          jsonPath: .spec.instances
        - name: Ready
          type: integer
          jsonPath: .status.readyInstances
        - name: Phase
          type: string
          jsonPath: .status.phase
        - name: Image
          type: string
          jsonPath: .spec.imageName
          priority: 1
      schema:
        openAPIV3Schema:
          description: FirebirdCluster is the Schema for the firebirdclusters API
          type: object
          required:
            - spec
          properties:
            apiVersion:
              description: >
                APIVersion defines the versioned schema of this representation of an object.
              type: string
            kind:
              description: Kind is a string value representing the REST resource this object represents.
              type: string
            metadata:
              type: object
            spec:
              description: FirebirdClusterSpec defines the desired state of a FirebirdCluster
              type: object
              required:
                - instances
                - storage
              properties:
                instances:
                  description: Number of Firebird instances to run in this cluster
                  type: integer
                  minimum: 1
                  maximum: 10
                imageName:
                  description: >
                    Full name of the Firebird Docker image to use.
                    Defaults to firebirdsql/firebird:latest
                  type: string
                superuserSecret:
                  description: Reference to the Secret containing the SYSDBA password
                  type: object
                  required:
                    - name
                  properties:
                    name:
                      description: Name of the Secret resource
                      type: string
                storage:
                  description: Storage configuration for Firebird data files
                  type: object
                  required:
                    - size
                  properties:
                    size:
                      description: Size of the PersistentVolumeClaim (e.g. "1Gi")
                      type: string
                    storageClass:
                      description: Optional StorageClass name for the PVC
                      type: string
                resources:
                  description: Resource requirements for each Firebird container
                  type: object
                  properties:
                    limits:
                      type: object
                      properties:
                        cpu:
                          type: string
                        memory:
                          type: string
                    requests:
                      type: object
                      properties:
                        cpu:
                          type: string
                        memory:
                          type: string
                backup:
                  description: Backup configuration
                  type: object
                  required:
                    - enabled
                  properties:
                    enabled:
                      description: Whether backups are enabled
                      type: boolean
                    schedule:
                      description: Cron schedule for backups (e.g. "0 2 * * *")
                      type: string
                    retentionPolicy:
                      description: Backup retention policy (e.g. "7d")
                      type: string
                monitoring:
                  description: Monitoring configuration
                  type: object
                  properties:
                    enablePodMonitor:
                      description: Whether to enable Prometheus PodMonitor
                      type: boolean
                replication:
                  description: Replication configuration (requires Firebird 4.0+)
                  type: object
                  required:
                    - enabled
                  properties:
                    enabled:
                      description: Whether replication is enabled
                      type: boolean
                    mode:
                      description: >
                        Replication mode. 'sync' waits for replica acknowledgement before
                        confirming writes (safer, slower). 'async' confirms writes immediately
                        and lets replicas catch up (faster, less durable). Defaults to 'async'.
                      type: string
                      enum:
                        - sync
                        - async
                env:
                  description: Additional environment variables for the Firebird container
                  type: array
                  items:
                    type: object
                    required:
                      - name
                    properties:
                      name:
                        type: string
                      value:
                        type: string
                      valueFrom:
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
            status:
              description: FirebirdClusterStatus defines the observed state of a FirebirdCluster
              type: object
              properties:
                instances:
                  description: Total number of configured instances
                  type: integer
                readyInstances:
                  description: Number of ready instances
                  type: integer
                phase:
                  description: Current phase of the cluster
                  type: string
                  enum:
                    - Creating
                    - Running
                    - Updating
                    - Degraded
                    - Deleting
                phaseReason:
                  description: Human-readable message about the current phase
                  type: string
                conditions:
                  description: List of status conditions
                  type: array
                  items:
                    type: object
                    required:
                      - type
                      - status
                      - reason
                      - message
                      - lastTransitionTime
                    properties:
                      type:
                        type: string
                      status:
                        type: string
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                      reason:
                        type: string
                      message:
                        type: string
                      lastTransitionTime:
                        type: string
                        format: date-time
